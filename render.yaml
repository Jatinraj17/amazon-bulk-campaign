services:
  - type: web
    name: amazon-bulk-campaign
    env: python
    buildCommand: pip install -e .
    startCommand: streamlit run streamlit_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
